[tool.pytest.ini_options]
pythonpath = ["custom_components/predistribuce"]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["pre_api"]

[tool.coverage.report]
show_missing = true
