[tool.pytest.ini_options]
pythonpath = ["custom_components/predistribuce", "."]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
include = ["**/pre_api.py"]

[tool.coverage.report]
show_missing = true
